{
  "scripts": {
    "test": "snoka run",
    "test:dev": "snoka open",
    "coverage": "snoka run --coverage",
    "server": "nodemon ../../packages/server/dist/standalone.js --watch ../../packages --delay 200ms -e js --ignore tests/ --nexus-artifacts"
  },
  "dependencies": {
    "@vue/test-utils": "^2.0.2",
    "nodemon": "^2.0.20",
    "vue": "^3.2.45"
  },
  "devDependencies": {
    "@snoka/server": "workspace:*",
    "@snoka/test": "workspace:*",
    "@vitejs/plugin-vue": "^4.0.0",
    "typescript": "^4.9.4",
    "vite": "^4.0.4",
    "vue-tsc": "^1.0.24"
  }
}
